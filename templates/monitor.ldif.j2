{{ ansible_managed | comment }}

dn: olcDatabase={1}monitor,cn=config
changetype: modify
replace: olcAccess
olcAccess: {0}to * by dn.base="gidNumber=0+uidNumber=0,cn=peercred,cn=external, cn=auth" read by dn.base="cn={{ openldap_server_admin }},dc={{ openldap_server_domain }},dc={{ openldap_server_tld }}" read by * none
olcAccess: {1}to attrs=userPassword by self write by anonymous auth by dn.base="cn={{ openldap_server_admin }},dc={{ openldap_server_domain }},dc={{ openldap_server_tld }}" write by * none
olcAccess: {2}to attrs=shadowLastChange by self write by * read
